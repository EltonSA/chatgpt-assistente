<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meus Assistentes</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4f7f6;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .navbar {
      background-color: #007bff;
    }
    .navbar-brand {
      color: #ffffff;
      font-weight: bold;
    }
    .navbar-brand:hover {
      color: #f8f9fa;
    }
    .container {
      padding-top: 3rem;
    }
    .card {
      border: none;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      margin-bottom: 1.5rem;
      background-color: #ffffff;
      position: relative;
    }
    .card-header {
      background-color: #007bff;
      color: white;
      border-radius: 12px 12px 0 0;
      padding: 1rem;
      font-weight: bold;
    }
    .card-body {
      padding: 2rem;
    }
    .card-title {
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }
    .card-text {
      color: #6c757d;
    }
    .badge-connected {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background-color: #28a745;
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 50px;
      font-size: 0.75rem;
    }
    .whatsapp-icon {
      font-size: 1.5rem;
      color: #25d366;
      margin-right: 0.5rem;
    }
    .btn-back {
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 0.375rem;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      margin-bottom: 1rem;
    }
    .btn-back:hover {
      background-color: #0056b3;
      color: white;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="/">Assistente de Atendimento</a>
  </nav>
  <div class="container">
    <a href="/" class="btn btn-back">
      <i class="fas fa-arrow-left"></i> Voltar
    </a>
    <h1 class="text-center mb-4">Meus Assistentes</h1>
    <div class="row">
      <% assistentes.forEach(assistente => { %>
        <div class="col-md-4">
          <div class="card">
            <div class="badge-connected">Conectado</div>
            <div class="card-header">
              <i class="fab fa-whatsapp whatsapp-icon"></i>
              WhatsApp
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= assistente.treinamento.split('\n')[0].split(': ')[1] %></h5>
              <p class="card-text"><%= assistente.treinamento.split('\n').slice(1).join('\n') %></p>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
